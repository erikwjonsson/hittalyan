%form.padhack.box.opaque(name="signup" ng-submit="submit()" novalidate)
  %fieldset
    %legend Registrera dig här!
    %label(for="email") Din e-postadress:
    %input(type="email"
           name="email"
           id="email"
           ng-model="email"
           required)
    %span(ng-show="signup.email.$invalid && signup.email.$dirty") Ogiltig e-postadress.
    
    %label(for="password") Lösenord:
    %input(type="password"
           name="password"
           id="password"
           ng-model="password"
           required
           ng-minlength="6"
           ng-maxlength="36")
    %span(ng-show="signup.password.$error.minlength") Lösenordet är för kort.
    %span(ng-show="signup.password.$error.maxlength") Lösenordet är för långt.
    %br
    %input(type="submit"
           class="btn"
           name="signup"
           value="Registrera")
