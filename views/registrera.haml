.row
  .span8.offset2.box.opaque
    .padhack
      %h2 Bli medlem här!
      :markdown
        Att bli medlem är gratis. 
        Som ny medlem får du tillgång till en begränsad form av tjänsten gratis.
        Det gör att du kan kolla runt "bakom kulisserna" och se vad du får om du väljer att köpa en prenumeration på lägenhetstips.

        Allt du behöver göra är att fylla i din e-postadress och det lösenord du vill ha. 
      %form(name="signup" ng-submit="submit()" novalidate)
        %fieldset
          %label(for="email")
            %strong Din e-postadress:
          %input(type="email"
                 name="email"
                 placeholder="E-postadress"
                 id="email"
                 ng-model="email"
                 required)
          %span(ng-show="signup.email.$invalid && signup.email.$dirty") Ogiltig e-postadress.
          %span(ng-show="signup.email.$valid") 
            %i.icon-ok.checkmark.okness.icon-large
          
          %label(for="password")
            %strong Önskat lösenord:
          %input(type="password"
                 name="password"
                 placeholder="Lösenord"
                 id="password"
                 ng-model="password"
                 required
                 ng-minlength="6"
                 ng-maxlength="36")
          %span(ng-show="signup.password.$error.minlength") Lösenordet är för kort.
          %span(ng-show="signup.password.$error.maxlength") Lösenordet är för långt.
          %span(ng-show="signup.password.$valid") 
            %i.icon-ok.checkmark.okness.icon-large
          %br
          %input(type="submit"
                 class="btn"
                 name="signup"
                 value="Bli medlem")
          %span(ng-show="working")
            %i.icon-spinner.checkmark.icon-spin.icon-large
            {{message}}
          %span(ng-show="cross")
            %i.icon-remove.checkmark.wrongness.icon-large
            {{message}}
