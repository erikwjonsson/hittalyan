.row
  .span8.offset2.box.opaque
    .padhack
      .alert.alert-info.text-center
        Redan medlem? <a href="/#!/login">Logga in här</a>
      %form.form-horizontal(name="signup" ng-submit="submit()" novalidate)
        %fieldset
          %legend Bli medlem
          :markdown
            Det är enkelt att börja använda HittaLyan.
            Du behöver inte fylla i något annat än din e-postadress och det lösenord som du vill ha för att logga in 
            på våra medlemssidor.

            När du tryckt på knappen aktiverar vi en prenumeration som gäller i 7 dagar.
            Du får tillgång till medlemssidorna där kan du ställa in vilka lägenheter du är intresserad av 
            och hur vi ska meddela dig om nya lägenheter. 
        .control-group
          %label.control-label(for="email")
            %strong Din e-postadress:
          .controls
            %input(type="email"
                   name="email"
                   placeholder="E-postadress"
                   id="email"
                   ng-model="email"
                   required)
            %span(ng-show="signup.email.$invalid && signup.email.$dirty") Ogiltig&nbsp;e-postadress.
            %span(ng-show="signup.email.$valid") 
              %i.icon-ok.checkmark.okness.icon-large
        .control-group
          %label.control-label(for="password")
            %strong Önskat lösenord:
          .controls
            %input(type="password"
                   name="password"
                   placeholder="Lösenord"
                   id="password"
                   ng-model="password"
                   required
                   ng-minlength="6"
                   ng-maxlength="36")
            %span(ng-show="signup.password.$error.minlength") Lösenordet&nbsp;är&nbsp;för&nbsp;kort.
            %span(ng-show="signup.password.$error.maxlength") Lösenordet&nbsp;är&nbsp;för&nbsp;långt.
            %span(ng-show="signup.password.$valid") 
              %i.icon-ok.checkmark.okness.icon-large
        .control-group
          .controls
            %input(type="submit"
                   class="btn"
                   name="signup"i
                   value="Bli medlem")
            %span(ng-show="working")
              %i.icon-spinner.checkmark.icon-spin.icon-large
              {{message}}
            %span(ng-show="cross")
              %i.icon-remove.checkmark.wrongness.icon-large
              {{message}}
