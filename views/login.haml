.row
  .span8.offset2.box.opaque
    .padhack
      %h2 Logga in här
      %form(name="login" ng-submit="submit()" novalidate)
        %fieldset
          %label(for="email") 
            %strong Din e-postadress:
          %input(type="email"
                 name="email"
                 placeholder="E-postadress"
                 id="email"
                 ng-model="email"
                 required)
          %span(ng-show="login.email.$invalid && signup.email.$dirty") Ogiltig e-postadress.
          
          %label(for="password") 
            %strong Lösenord:
          %input(type="password"
                 name="password"
                 placeholder="Lösenord"
                 id="password"
                 ng-model="password"
                 required
                 ng-minlength="6"
                 ng-maxlength="36")
          %span(ng-show="login.password.$error.minlength") Lösenordet är för kort.
          %span(ng-show="login.password.$error.maxlength") Lösenordet är för långt.
          %br
          %input(type="submit"
                 class="btn"
                 name="login"
                 value="Logga in")
          %span(ng-show="working")
          %span(ng-bind-html="feedBackSymbol")
          %span {{message}}
