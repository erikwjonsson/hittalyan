%form(name="login" ng-submit="submit()" novalidate)
  %fieldset
    %legend Logga in här
    %label(for="email") Din e-postadress:
    %input(type="email"
           name="email"
           placeholder="E-postadress"
           id="email"
           ng-model="email"
           required)
    %span(ng-show="login.email.$invalid") Ogiltig e-postadress.
    
    %label(for="password") Lösenord:
    %input(type="password"
           name="password"
           placeholder="Lösenord"
           id="password"
           ng-model="password"
           required
           ng-minlength="6"
           ng-maxlength="36")
    %span(ng-show="login.password.$error.minlength") Lösenordet är för kort.
    %span(ng-show="login.password.$error.maxlength") Lösenordet är för långt.
    
    %input(type="submit"
           class="btn"
           name="login"
           value="Logga in")
