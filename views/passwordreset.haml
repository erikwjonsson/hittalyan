%form(name="passwordreset" ng-submit="submit()" novalidate)
  %fieldset
    %legend Lösenordsåtertällning
    %label(for="email") Din e-postadress:
    %input(type="email"
           name="email"
           id="email"
           ng-model="email"
           required)
    %span(ng-show="signup.email.$invalid && signup.email.$dirty") Invalid email
    %input(type="submit"
           class="btn"
           name="signup"
           value="Down the rabbit hole")
%p
  Du kommer få ett mail med en länk som du måste klicka för att bekräfta lösenordsåterställningen.<br/>
  Du kommer då få ett nytt mail med ditt nya lösenord som du kan använda för att logga in med.<br>
  Du kommer kunna ändra löenordet till något bättre via Medlemssidan.<br/>
  Du kommer ge mig alla dina pengar.<br/>